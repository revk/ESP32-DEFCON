gpio	lights			.array=5		// GPIO for each DEFCON level light
gpio	clicker						// GPIO for relay clicker
gpio	beeper						// GPIO for beeper
gpio	blinker						// GPIO for 1Hz blinker
gpio	rgb						// GPIO for RGB led strip
u8	leds		11				// How many LED strip LEDs (allow at least 9)
u8	defcon.blink	10				// Level below which the blinker is on
u8	defcon.click	6				// Level below which clicking is done
u8	defcon.beep	5				// Level below which beeping is done
s	topic		DEFCON				// The base topic
